# 跨项目通用 AGENTS 模板

> 本文件是一份可复制的通用模板。文件名为 `AGENTS.universal.md` 时通常不会被代理自动加载；复制到目标项目后，应按所用工具的规则重命名为 `AGENTS.md`。如果项目已经存在 `AGENTS.md`，应先合并项目专用规则，不要直接覆盖。

## 1. 使用方式与适用范围

- 本文件的主体规则适用于代码、文档、配置、数据、脚本、构建系统和资源文件等常见项目。
- “条件规则”只在仓库具备对应技术栈或运行环境时启用，不得无条件套用。
- 项目内更接近目标文件的嵌套 `AGENTS.md` 只约束其所在目录及子目录，并覆盖上层文件中的冲突规则。
- 复制本模板后，应优先填写“项目适配区”。未填写的事实必须从仓库中发现，不得凭经验猜测。
- 本模板不替代系统、平台、安全或用户指令。

## 2. 指令优先级

发生冲突时，按以下顺序执行：

1. 系统、平台和安全策略。
2. 开发者指令和用户当前明确要求。
3. 目标文件所在目录或最近父目录中的 `AGENTS.md`。
4. 更上层目录中的 `AGENTS.md`。
5. 项目文档、构建配置、CI 配置和既有测试约定。
6. 本文件中的通用默认规则。
7. 相邻代码已经形成的一致惯例。

如果高优先级指令明确要求跳过构建、测试、格式化、联网或外部写操作，应以高优先级指令为准，并在交付时说明未执行项。

## 3. 项目适配区

复制到新项目后填写下列信息。未填写时，先从 `README`、构建清单、包管理文件、CI、源码入口和现有测试中自动发现；只有无法发现且会实质改变结果时才询问用户。

- 项目目标与主要用户：`<待填写或自动发现>`
- 技术栈与语言版本：`<待填写或自动发现>`
- 支持的平台、架构和编译器：`<待填写或自动发现>`
- 主要源码、测试、资源和文档目录：`<待填写或自动发现>`
- 架构边界和权威数据源：`<待填写或自动发现>`
- 构建命令及目标：`<待填写或自动发现>`
- 测试、静态检查和格式检查命令：`<待填写或自动发现>`
- 生成文件、数据库结构和迁移规则：`<待填写或自动发现>`
- 禁止或需要确认的操作：`<待填写或自动发现>`
- 发布、提交和部署规则：`<待填写或自动发现>`

项目适配区中的明确规则优先于后文的通用默认规则。

## 4. 工作方式

### 4.1 先理解任务类型

- 回答、解释、审查或报告状态：以只读检查为主，不主动修改文件、外部系统或 Git 状态。
- 诊断问题：找出原因并提供证据；除非用户同时要求修复，否则不要擅自实现。
- 修改或构建：完成请求范围内的实现，执行与风险相称的验证，并交付可核查结果。
- 等待或监控：使用项目或平台提供的等待机制，不通过高频轮询制造负载。
- 用户在处理中追加要求时，判断是替换原任务还是补充原任务；补充要求应与尚未完成的工作一起处理。

### 4.2 开始前

- 阅读当前目录及父目录中适用的 `AGENTS.md`，并跟随其中引用的必要规则。
- 检查仓库状态、相关文档、入口、构建清单、依赖配置和测试。
- 使用搜索确认是否已经存在相同能力、接口、工具类、组件、服务、数据模型或线程封装。
- 区分用户已有修改与本次任务修改。现有未提交或未跟踪内容默认属于用户。
- 如果大范围修改会与现有未提交更改重叠、混入同一提交或难以安全隔离，应先说明风险并请求方向。
- 对可从仓库发现的事实先搜索，不向用户询问路径、类型、接口等可发现信息。

### 4.3 实施中

- 优先局部修改，保持既有架构、命名、代码风格和数据布局。
- 优先扩展现有类、模块、事件、协议、路由、服务和测试能力。
- 不因“顺手整理”而批量改名、格式化或清理无关代码。
- 不修改无关文件，不覆盖用户更改，不把无关文件纳入提交。
- 未经明确要求，不引入第三方依赖、新框架、新基类体系、新线程封装或新的全局状态。
- 只有在确有复用价值且职责明确时才提取公共能力，避免为单次需求过度抽象。
- 对任务中合理且低风险的细节可做最小假设；会改变产品行为、数据兼容、公共接口或外部状态的假设必须明确说明。

### 4.4 完成后

- 检查实际差异、工作区状态和未预期文件。
- 执行与风险相称且项目允许的验证。
- 交付信息应先说明结果，再说明关键改动、验证范围、未执行项和剩余风险。
- 不得把未运行的测试写成“已通过”，不得用生成文件或旧构建输出代替本次验证。

## 5. 搜索、读取与工具选择

- 文件发现优先使用 `rg --files`，文本搜索优先使用 `rg -n`。
- 默认排除依赖、生成、缓存、构建和版本控制目录，例如：

```text
!**/node_modules/**
!**/vendor/**
!**/dist/**
!**/out/**
!**/target/**
!**/cmake-build-*/**
!**/build/**
!**/.git/**
```

- 优先读取相关区间而不是整份大文件；需要掌握完整规则、协议或短文件时再读取全文。
- 优先使用项目已有的结构化工具、脚本和解析器，不用正则或手工字符串处理替代可靠的语法、JSON、XML、表格或数据库工具。
- 搜索顺序通常为：项目规则与文档、相邻实现、公共模块、调用点、测试、构建登记、历史差异。
- 命令保持单一目的，避免用长命令链混合发现、修改、删除和验证。
- 不使用可能展开未解析变量、宽泛通配符或命令替换的危险路径。
- 手工编辑优先使用补丁方式；简单编辑不使用临时脚本绕过可审查差异。
- 格式化、代码生成和批量机械改写可以使用项目工具，但必须先确认作用范围且不得改写无关文件。

## 6. 代码与架构规则

### 6.1 架构与职责

- 先遵守现有模块边界。界面、业务、数据访问、设备通信、协议、存储和基础设施应保持职责分离。
- 顶层入口只负责生命周期、装配和协调，不承载可独立封装的复杂业务。
- 阻塞 I/O、长时间计算、协议解析和持久化不得放入界面事件处理函数。
- 跨模块协作优先使用既有接口、事件、消息、路由或服务，不通过全局对象或跨层直接访问内部状态形成耦合。
- 新增文件、资源、测试或目标时，同步检查构建系统、包清单、资源清单和安装规则。

### 6.2 接口与兼容

- 除非任务明确要求，不修改公共接口、持久化结构、通信布局、命令行参数或公开配置格式。
- 修改公共接口时，同时检查所有调用点、实现、测试、序列化、文档及向后兼容影响。
- 扩展协议或二进制结构时明确地址、长度、字节序、对齐、版本、单位、范围和保留字段。
- 不假定本机字长、字节序、内存对齐、文件系统大小写规则或默认字符编码。
- 外部输入必须在更新运行状态前校验类型、存在性、长度、范围、枚举值和版本。
- 错误分支应保留可诊断信息，并向调用方提供与现有接口一致的失败结果。

### 6.3 命名、函数和注释

- 命名直接表达业务语义，避免只有作者才能理解的缩写。
- 布尔变量和函数优先使用 `is`、`has`、`can`、`should` 等可读前缀。
- 类相关逻辑放入职责明确的类；仅供单个类使用的逻辑优先作为私有成员。
- 非必要不新增全局函数、全局变量、单例或文件级静态状态。
- 声明与实现顺序保持一致；头文件或公共模块保持最小依赖。
- 注释解释非直观的约束、原因、所有权和协议语义，不重复代码表面行为。
- 关键协议字段、单位、时间基准、状态机、数据库字段和资源所有权必须有简洁注释。

### 6.4 依赖和生成内容

- 优先使用项目已经引入的依赖和标准能力。
- 新增依赖前确认许可证、平台支持、版本锁定、体积、安全和构建影响，并获得任务范围内的明确授权。
- 不直接修改可由工具重新生成的文件；应修改权威源并使用项目既有生成流程。
- 涉及数据库结构、迁移、实体生成配置或不可逆数据转换时，先评估兼容和回滚策略；任务未明确授权时应请求确认。

## 7. 数据、文件、日志与安全

- JSON、YAML、XML、CSV、表格、配置和协议数据使用结构化 API 读写，不手工拼接易出错的格式。
- 文件写入应处理路径、编码、打开、截断、原子替换、部分写入、权限、磁盘空间和失败清理。
- 大文件、设备 I/O 和网络传输应避免一次性无界读取或阻塞 UI/主事件线程。
- 日志延续项目现有体系和分级，关键失败包含操作对象、阶段及可行动的原因。
- 不在日志、命令输出、测试快照或交付文本中泄露密码、令牌、私钥、个人信息或无界二进制数据。
- 不将秘密写入仓库；发现疑似秘密时不要回显其完整内容。
- 临时文件应放在明确的临时目录，任务结束后按安全规则清理。
- 外部输入、文件名、归档内容和网络响应均视为不可信，防止路径穿越、命令注入、越界和资源耗尽。

## 8. 并发、异步与资源生命周期

- 明确对象、线程、任务、计时器、回复、句柄、文件和连接的所有权。
- 所有启动路径都应有可重复调用的停止和清理路径，包括初始化部分失败的情况。
- 后台任务必须支持取消或安全退出；阻塞等待需要可被退出请求唤醒。
- 不让异步回调捕获可能悬空的对象、引用或界面指针。
- 不在后台线程直接操作 UI；通过项目既有调度、事件或消息机制回到 UI 线程。
- 共享可变状态使用与项目一致的锁、原子、消息传递或线程封闭策略。
- 锁的范围应最小且顺序明确，不在持锁状态执行未知回调、阻塞 I/O 或跨线程同步调用。
- 重连、重试、订阅和信号连接必须避免重复注册、请求堆积和无限快速重试。
- 重试应定义次数、间隔、超时、幂等性和最终失败状态；不得掩盖永久错误。

## 9. 跨平台规则

- 从项目配置确认支持的平台、架构、编译器和运行时，不为当前机器删除其他平台能力。
- 平台相关 API 使用项目既有条件编译或适配层隔离。
- 公共模块不得无条件引入单一平台专有头文件、系统调用或路径格式。
- 检查字符编码、换行、路径分隔符、文件名大小写、时间表示、整数宽度和序列化字节序。
- 条件编译的每个有效分支都应保持声明、类型和返回语义一致。
- 无法在当前环境验证的平台分支应做静态检查并在交付时明确说明。

## 10. 破坏性操作与外部副作用

- 删除、覆盖、迁移、清库、强制重建、历史改写、发布、部署和发送外部消息前，确认动作在用户授权范围内。
- 执行递归删除或移动前，解析并检查绝对目标，确保其位于明确的项目或测试目录内。
- 不对仓库根目录、用户目录、磁盘根目录或未解析变量执行递归破坏性命令。
- 优先选择可恢复操作；删除重要内容后说明删除对象和恢复可能性。
- 不通过切换 shell、拼接字符串、宽泛通配符或未校验脚本规避安全检查。
- 外部系统写操作只影响用户明确放入任务范围的系统、数据和对象。

## 11. Git 与工作区

- 开始和结束时检查 `git status --short`；需要理解修改时查看相关文件差异。
- 现有修改和未跟踪文件默认属于用户，不删除、不覆盖、不格式化、不移动。
- 不使用 `git reset --hard`、`git checkout -- <path>` 等破坏性恢复命令，除非用户明确要求且目标已经核实。
- 未经要求，不创建分支、不暂存、不提交、不变基、不合并、不推送、不创建拉取请求。
- 用户只要求“提交”时，默认仅创建本地提交；只有明确要求时才推送。
- 暂存或提交时只包含本任务文件，提交前复核暂存差异。
- 不把依赖目录、构建产物、缓存、秘密、日志或临时文件纳入版本控制。
- 如果任务修改范围较大且与已有未提交更改重叠，先说明混合风险并确认处理方式。

## 12. 验证策略

### 12.1 优先级

- 用户或项目规则明确指定的验证方式优先。
- 用户要求“只做静态检查”或“不要构建/测试”时，不得运行构建和测试。
- 未明确禁止时，根据项目类型、现有环境和变更风险选择最小充分验证。
- 不为了验证擅自创建新的大型构建目录、下载依赖、修改锁文件或改写快照。
- 优先使用已有构建目录、缓存、配置和项目脚本。

### 12.2 风险分级矩阵

| 等级 | 典型变更 | 最低验证 |
| --- | --- | --- |
| L0 | 解释、只读审查、纯文本说明 | 事实核对、引用位置、说明未运行项 |
| L1 | 文档、注释、局部配置、低风险静态修改 | 差异检查、格式/语法静态检查、相关搜索 |
| L2 | 局部业务逻辑、错误处理、普通 UI 行为 | L1 + 最相关测试或静态分析 |
| L3 | 公共接口、协议、并发、持久化、跨模块行为 | L2 + 受影响目标构建及针对性测试 |
| L4 | 数据迁移、安全、发布、启动链路、跨平台核心 | L3 + 集成/端到端验证、回滚或兼容检查 |

项目无法提供某级验证条件时，执行可行的低一级检查并明确剩余风险，不虚构结果。

### 12.3 通用检查清单

- 声明、定义、调用点和接口签名一致。
- 空指针、越界、整数溢出、编码、错误返回和异常路径安全。
- 并发访问、取消、退出、对象所有权和资源释放完整。
- 平台条件分支、依赖、资源、构建登记和安装规则完整。
- 新增测试覆盖正常、边界、失败、恢复和重复调用场景。
- `git diff --check` 无空白错误；实际差异没有无关改动。
- 工作区没有意外生成或修改的文件。

### 12.4 构建与测试

- 从项目文档、CI、包清单和已有构建配置发现正确命令。
- 优先构建受影响目标并运行最相关测试，再根据风险扩大范围。
- 不把已有缓存、旧二进制或历史日志当成本次验证证据。
- 构建或测试失败时区分代码问题、环境问题和既有失败，并保留关键错误信息。
- 如果验证工具会更新源码、快照、锁文件或生成文件，运行前确认这些写入属于任务范围。

## 13. 代码审查与交付格式

### 13.1 审查

- 审查报告先列问题，按严重度和风险排序。
- 每个问题包含文件位置、触发条件、影响、原因和可执行修复建议。
- 区分已证实事实、合理推断和待确认假设。
- 优先报告正确性、安全、数据丢失、死锁、兼容和资源泄漏问题，不用大量风格意见掩盖高风险问题。
- 没有发现问题时明确说明，同时列出审查范围和未执行验证。

### 13.2 交付

- 首句说明任务是否完成以及用户可直接获得的结果。
- 简述关键行为变化，不罗列无意义的编辑过程。
- 提供重要文件或入口位置，避免重复引用同一文件。
- 列出实际执行的检查、测试和构建结果。
- 明确未执行项、环境限制、假设和仍需用户处理的事项。
- 不声称未验证的外部状态已经成功，例如部署、推送、设备重启或消息送达。

## 14. 条件附录：Qt/C++

仅当仓库通过 `CMakeLists.txt`、`.pro`、Qt 包配置、源码包含或项目文档表明使用 Qt/C++ 时启用本节。

### 14.1 版本与类型

- 从构建配置确认 Qt、C++ 标准和编译器版本，不主动添加无需求的跨版本兼容写法。
- 与 Qt API、信号槽、元对象、`QVariant`、model/view 和界面交互时优先保持项目现有 Qt 类型体系。
- 标准库能力明显更合适或现有上下文已经使用标准库时，可局部使用标准类型，避免无意义转换。
- 不因个人偏好将未废弃的 Qt 容器和工具类型批量替换为标准库类型。

### 14.2 QObject、信号槽和元类型

- 为动态创建的 `QObject` 明确父对象，或提供唯一且清晰的删除路径。
- 异步对象、设备回复和可能提前销毁的对象使用项目认可的安全引用策略。
- 自定义类型跨线程、进入 `QVariant` 或事件系统前，按项目方式声明并注册元类型。
- 信号槽连接必须有正确的接收者上下文；跨线程连接明确使用适当的 queued 语义。
- 可能重复建立的连接使用唯一连接或在重连前断开，避免重复回调。
- 同步完成和异步完成的回复都检查错误并释放。

### 14.3 线程

- UI 只在 GUI 线程更新。
- 一次性后台任务优先使用项目已有的 `QtConcurrent`、任务系统或受控线程创建方式。
- 常驻线程优先使用项目已有线程管理封装，不另建相互竞争的生命周期体系。
- 线程退出时停止定时器、唤醒等待、取消或清理回复、断开设备并结束事件循环。
- `deleteLater()` 必须在对象所属线程仍能处理事件时调用；不能依赖已停止事件循环处理清理。
- lambda 不捕获生命周期短于任务的裸 `this`、引用或 UI 对象。

### 14.4 UI、模型和资源

- QWidget/QML 只负责展示、输入校验和用户交互；耗时 I/O 和长期流程放入服务或工作对象。
- 复用现有页面、路由、事件、对话框、组件和主题系统。
- Model、Delegate 与 View 保持职责边界；数据变化发出正确模型通知。
- 新增 `.ui`、QML、图片、翻译或其他资源时同步更新资源清单、构建清单和安装规则。
- 资源通过 Qt 资源系统或项目既有定位方式访问，不硬编码开发机路径。

### 14.5 平台与构建

- 平台相关实现使用项目现有适配层或 `Q_OS_*` 条件宏，公共代码不无条件包含平台专有 API。
- 序列化和设备通信不假定 x86 字节序、自然对齐或桌面平台字长。
- 允许构建时优先使用已有 CMake/qmake 配置和受影响目标，不擅自从零创建新构建目录。
- 使用 IDE 配置的项目，应从现有配置读取生成选项、工具链、环境变量和依赖路径，不硬编码本机安装位置。

## 15. 条件附录：RTK 与 Windows PowerShell

仅当更高层规则要求使用 `rtk`，或当前环境已安装并将其作为命令代理时启用本节。若 `rtk` 不可用且没有强制要求，使用当前平台的原生命令并保持同等安全约束。

### 15.1 RTK

- 所有 shell 命令使用 `rtk` 前缀。
- 外部可执行程序直接通过 `rtk` 调用，例如：

```powershell
rtk git status --short
rtk rg -n "<PATTERN>" "<PROJECT_ROOT>"
rtk <BUILD_TOOL> <ARGS>
```

- PowerShell cmdlet 通过以下形式执行：

```powershell
rtk pwsh -NoProfile -Command '<SCRIPT>'
```

- 含 `$variable`、`$_` 等 PowerShell 变量的脚本使用单引号包围 `-Command` 内容。
- `rtk gain`、`rtk --version` 等元命令只用于诊断代理本身，不代替项目验证。

### 15.2 Windows 命令规则

- 使用 `pwsh`，不使用旧版 `powershell.exe`。
- 路径操作优先使用 `-LiteralPath`，尤其是包含空格、方括号或非 ASCII 字符的路径。
- 不在 PowerShell 中使用 Bash 的 `&&`、`||`、重定向约定或嵌套引号写法。
- 不假定 `cat`、`sed`、`grep`、`head`、`tail` 具有 Unix 语义；优先使用 `rg` 或 PowerShell cmdlet。
- `rg` 返回 1 可能只表示无匹配；仅在“无匹配”属于允许结果时规范化该退出码。
- 删除或移动操作必须在同一 shell 中完成路径解析、范围检查和执行，不把枚举结果传给另一 shell。

常用只读命令示例：

```powershell
rtk rg --files "<PROJECT_ROOT>" -g "!**/node_modules/**" -g "!**/cmake-build-*/**" -g "!**/build/**" -g "!**/.git/**"
rtk rg -n "<PATTERN>" "<PROJECT_ROOT>" -g "!**/node_modules/**" -g "!**/cmake-build-*/**" -g "!**/build/**" -g "!**/.git/**"
rtk pwsh -NoProfile -Command 'Get-Content -LiteralPath "<FILE>" -TotalCount 120'
rtk git -C "<PROJECT_ROOT>" status --short
rtk git -C "<PROJECT_ROOT>" diff --check
```

允许无匹配的搜索示例：

```powershell
rtk pwsh -NoProfile -Command 'rg -n "<PATTERN>" "<PATH>"; if ($LASTEXITCODE -eq 1) { exit 0 } else { exit $LASTEXITCODE }'
```

## 16. 模板维护

- 项目专用信息应写入项目适配区或独立的项目级 `AGENTS.md`，不要污染通用主体。
- 新增条件附录时必须定义明确的启用条件，并保证未使用该技术栈的项目不会受到约束。
- 修改规则时检查是否与更高优先级、安全策略或其他章节重复冲突。
- 定期删除已经失效的工具命令，但保留仍具普适性的行为和安全原则。
